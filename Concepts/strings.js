//String Interpolation : `${placeholder}`

const name = new String("Ashish");

console.log(name);

// substring = (start, end);
//slice = (start- can be negative or starting from reverse of a string, end);
//trim = removes spaces from starting as well ending.
//replace = ('element to be replaced', 'replacement element');
// includes =  checks whether some element are present in string or not  //includes()
// split = it gives an array of string subelements // split(separator, limit)
